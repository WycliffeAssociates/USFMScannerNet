name: PR Build

on:
  pull_request:
    branches:
      - develop

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'

    - name: Restore dependencies
      run: dotnet restore UsfmScannerNet.sln

    - name: Build
      run: dotnet build UsfmScannerNet.sln --configuration Release --no-restore

    - name: Test
      run: dotnet test UsfmScannerNet.Tests/UsfmScannerNet.Tests.csproj --configuration Release --no-build
